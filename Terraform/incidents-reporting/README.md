## Project: 
### Build a small web application where users can submit and track IT/security incidents. The application is containerised with Docker and automatically deployed to AWS through a CI/CD pipeline.
### Create a VPC